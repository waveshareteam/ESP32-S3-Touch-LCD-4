# 贡献指南

[English](CONTRIBUTING.md)

感谢改进本开发板仓库。请保持变更聚焦，更新受影响的文档，并让 GitHub Actions 按路径分类后验证受影响的 ESP-IDF 或 Arduino 示例。请使用仓库相对路径，不要提交 `build/`、`managed_components/`、`dependencies.lock` 或本地 `sdkconfig` 文件。

ESP-IDF 项目位于 `examples/esp-idf/`；Arduino 产品示例位于 `examples/arduino/`。随附库的上游示例不属于产品 CI，除非维护者明确扩大范围。
